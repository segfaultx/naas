apiVersion: v1
kind: Service
metadata:
  name: naas
  labels: naas
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{ .Values.service.name }}
